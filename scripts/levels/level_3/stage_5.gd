extends StageScreen

## Level 3 Stage 5 — the glowing fruit.
##
## The fruit hangs at mid height on the stem, and the low bubble at y 1000 clears
## it.
##
## This is Level 3's last stage, so its `next_stage_path` is empty and it leaves
## on `done_scene_path` — the hub for now, and the Level 3 ending once there is
## one.
##
## Built to Stage 1's numbers — header at 760 px, tap plank at 1478, three
## cards at y 1610 — and it changes only what is written above. See
## `scripts/levels/level_3/stage_1.gd` for how a Level 3 stage works.
